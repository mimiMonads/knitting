// Browser stub for `src/worker/process-worker-bootstrap.ts`. A page is never a
// process worker, so the real module's guards already make both of these
// no-ops there.
export const getProcessWorkerNativeWaitU32 = (): undefined => undefined;

export const installProcessWorkerBootstrap = (): void => {};
